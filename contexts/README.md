# The Frankenstyle Project — Context Files

This folder contains all the Markdown files you feed to the AI.

## Implementation vs. Usage

You will see file names or directories ending in `-implementation` or `-usage`. The implementation files are written for human readers, while the usage files are token-optimized to reduce LLM hallucinations. Use either set depending on your token budget and whether the model is confabulating.